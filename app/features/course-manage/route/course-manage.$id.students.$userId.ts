import { courseStudentDetailLoader } from "~/features/course-manage/services/course-student-detail.loader";

/** Resource route: the progress dialog's fetcher target. No UI of its own. */
export const loader = courseStudentDetailLoader;
